NamedElement has no outgoing dependencies. In the PCM and mPCM the relevant subgraph consists of only this class with only one module. Thus, the comparison does not yield information. Also, an actual problem is, that the analysis throws errors, as it is not designed for such small hypergraphs.