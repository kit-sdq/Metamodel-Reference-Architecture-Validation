From this project, the modules which now are the mPCM, were created.
In this pcm.ecore, the package structure already resembles the modules.
The dependencies, however, might still validate the layering.
They were fixed during the modularization process.